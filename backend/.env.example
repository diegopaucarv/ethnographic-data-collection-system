# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/ayni

# Security (generate a random value of at least 32 characters)
SECRET_KEY=replace-with-a-random-32-character-secret

# Frontend origins, comma-separated when more than one
CORS_ORIGINS=http://localhost:3000

# Server
PORT=8000
NODE_ENV=development
